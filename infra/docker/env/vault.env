# Redis configuration
REDIS_URL=redis://localhost:6379
# Alternatively, use Redis Sentinel
# REDIS_SENTINEL_HOSTS=host1:port1,host2:port2

# Encryption keys
ENCRYPTION_KEY=1f495ea8d9d7a2bfcfacb687a7d22a9e7122e6cdae63da49c045d60aee23dedf
# Alternatively, use ROOT_ENCRYPTION_KEY
# ROOT_ENCRYPTION_KEY=

# Authentication secret
AUTH_SECRET=5e0abb7134de41d113932de3a8d3528e374d0307d57141d293f4cfe0788c5e30

# PostgreSQL configuration
POSTGRES_USER=vault
POSTGRES_PASSWORD=vaultpassword
POSTGRES_DB=vault
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
# Official Infisical connection URI expected by the app
DB_CONNECTION_URI=postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}
# DATABASE_URL is intentionally omitted to avoid conflicts
